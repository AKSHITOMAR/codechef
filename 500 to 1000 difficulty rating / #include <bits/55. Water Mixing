#include <bits/stdc++.h>
using namespace std;
int main() {
int t,a,b,x,y;
cin>>t;
while(t--){
    cin>>a>>b>>x>>y;
    if(a==b){
        cout<<"yes"<<endl;
    }
    else if(a<b){
        int required=b-a;
        if(x>=required)
        cout<<"yes"<<endl;
        else cout<<"no"<<endl;
    }
    else {
        int required=a-b;
        if(y>=required) cout<<"yes"<<endl;
        else cout<<"no"<<endl;
    }
}
}

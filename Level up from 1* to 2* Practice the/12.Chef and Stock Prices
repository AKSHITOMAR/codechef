#include <bits/stdc++.h>
using namespace std;
int main() {
    int t,s,a,b,c;
    cin>>t;
    while(t--){
        cin>>s>>a>>b>>c;
       double add=s+(c/100.0*s);
        if(add>=a && add<=b){
            cout<<"yes"<<endl;
        }
        else{
            cout<<"no"<<endl;
        }
    }
}
